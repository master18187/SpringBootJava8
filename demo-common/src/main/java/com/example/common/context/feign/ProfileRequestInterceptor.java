package com.example.common.context.feign;

import feign.RequestInterceptor;
import feign.RequestTemplate;

public class ProfileRequestInterceptor implements RequestInterceptor {

    
    @Override
    public void apply(RequestTemplate requestTemplate) {

    }
}
